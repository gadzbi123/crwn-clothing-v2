import { SpinnerContainer, StyledSpinner } from "./spinner.styles";

const Spinner = () => {
  return (
    <SpinnerContainer>
      <StyledSpinner />
    </SpinnerContainer>
  );
};
export default Spinner;
